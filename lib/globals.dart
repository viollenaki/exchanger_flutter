String? user_id; // Stores the logged-in user's ID
String? username; // Stores the logged-in user's username
String? email; // Stores the logged-in user's email
Map? lastCurrency; // Stores the last selected currency
List? transactions; // Stores the user's transaction history
